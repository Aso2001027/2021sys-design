```uml
@startuml

!define MASTER_MARK_COLOR Orange 
!define TRANSACTION_MARK_COLOR DeepSkyBlue

skinparam class {
    /'図の背景'/
    BackgroundColor White/Orange
    /'図の枠'/
    BorderColor Black
    /'リレーションの色'/
    ArrowColor Black
}

package "ECサイト" as target_system {
    /'
      マスターテーブルを M、トランザクションを T などで表記
      １文字なら "主" とか "従" まど日本語でも記載可能
     '/

    entity "顧客マスタ" as Entity01 <m_customers> <<M,MASTER_MARK_COLOR>> {
        + customer_code [PK]
        --
        pass
        name
        address
        tel
        mail
        del_flag
        reg_date
    }
    entity "購入テーブル" as Entity02 <d_purchase> <<T,TRANSACTION_MARK_COLOR>> {
        + order_code [PK]
        --
        # customer_code[FK]
        purchase_date
        total_price
    }
     entity "購入詳細テーブル" as Entity03 <d_purchase_detail> <<T,TRANSACTION_MARK_COLOR>> {
        + order_id [PK]
        + detail_id[PK]
        --
        # item_code[FK]
        price
        num
     }
     entity "商品マスタ" as Entity04 <m_items> <<M,MASTER_MARK_COLOR>> {
        + item_code [PK]
        --
        item_name
        price
        # category_id[FK]
        image
        detail
        del_flag
        reg_date
    }
    entity "カテゴリマスタ" as Entity05 <m_category> <<M,MASTER_MARK_COLOR>> {
        + category_id [PK]
        --
        name
        reg_date
    }
    
    Entity01 |o-ri-o{ Entity02
    Entity02 ||-ri-|{ Entity03
    Entity03 }-do-|| Entity04
    Entity04 }o-le-|| Entity05
@enduml
```
